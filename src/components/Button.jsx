import React from "react";

const Button = ({children}) => {
  return (
    <button
      className="button-83 text-xl"
      role="button"
    >
      {children}
    </button>
  );
};

export default Button;
